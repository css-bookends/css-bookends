import { describe, expect, it } from 'vitest';

import * as gilding from '../src/index';

describe('@css-bookends/gilding (scaffold)', () => {
  it('loads as a module', () => {
    expect(gilding).toBeDefined();
  });
});
